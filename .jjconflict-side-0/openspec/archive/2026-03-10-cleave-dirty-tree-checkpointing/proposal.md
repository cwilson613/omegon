# Cleave dirty-tree checkpointing and lifecycle commit policy

## Intent

Assess whether repeated 'working tree has uncommitted changes' failures indicate a process gap between OpenSpec lifecycle state and git hygiene, and whether pi-kit should guide or enforce checkpoint commits/snapshots before /cleave begins.
